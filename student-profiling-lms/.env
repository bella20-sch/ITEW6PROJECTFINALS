# Dev: keep this unset so the app uses /api on port 3000 and Vite proxies to Express on 5000.
# If you see "Cannot POST /api/...", start the API (student-profiling-node) and restart: npm run dev
#
# Optional — call API directly (no proxy):
# VITE_API_BASE=http://localhost:5000
